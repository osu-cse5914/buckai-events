name = "social-osu"
main = "src/index.ts"
compatibility_date = "2025-03-01"

[assets]
directory = "../web/dist"
not_found_handling = "single-page-application"

# DATABASE_URL is stored as an encrypted secret, not a plain var.
# Set it once after login:
#   wrangler secret put DATABASE_URL
